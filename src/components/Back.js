import React, { Component } from 'react';
import '../App.css';

export default class Back extends Component {
  render() {
    return (
      <div>
        <button>Back</button>
      </div>
    );
  }
}